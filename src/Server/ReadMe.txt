This folder manages the Server side of the application. 

The Database folder contains the classes that the database needs to create, get, put, update or delete data from it.

The ServerHandler folder contains the classes to maintain the server so the user can connect and send data to it. 

The Test folder contains the JUnit testing done for the application.